---
const { href, className, ...props } = Astro.props;
---

<a href={href} class={`inline-block px-6 py-3 text-lg font-semibold rounded-lg transition-colors duration-200 ${className}`}
  {...props}
>
  <slot />
</a>
